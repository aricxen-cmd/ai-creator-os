export async function generateResearch(topic: string) {
  return `
# ${topic}

This is placeholder research.

Later this will call your AI provider and return
structured research that can be saved directly to the project.
`;
}