export interface ScriptOptions {
  topic: string;
  platform: string;
  length: string;
  style: string;
  audience: string;
  provider: string;
  model: string;
}

export interface ScriptResult {
  content: string;
}